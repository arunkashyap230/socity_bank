import SimplePage from "./SimplePage";

const Accounts = () => (
  <SimplePage
    title="Accounts"
    description="Review and manage member accounts and balances."
  />
);

export default Accounts;
